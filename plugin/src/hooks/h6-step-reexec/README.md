H6 handler — fires on PreToolUse for Write/Edit/Bash. Computes step identity (step_id + sha256 hash_of_inputs after PAT redaction) and consults the append-only `<plan_dir>/cdcc-step-history.jsonl`. On a re-execution match, requires a `Step-Re-Execution: gate-NN reason "<rationale>"` authorization in `<plan_dir>/cdcc-step-reexec-authorization.txt` or blocks (exit 1). Fresh steps are recorded; corrupt history lines are gracefully skipped. Tool-time forward defender for ASAE Aspect 13 / FM-1.3 elevation; pairs with commit-msg hook v05 Tier 1c-extended mode-conditional backstop. Spec: `_grand_repo/docs/CDCC_H6_Spec_2026-04-26_v01_I.md`.

PAT redaction patterns (bounded; see `step-identity.ts`): `ghp_*`, `github_pat_*`, `gho_*`, `ghs_*`, `ghu_*`, `Bearer <opaque>`, `AKIA*`. Generic 32+ char hex tokens NOT redacted (false-positive risk vs step_id stability tradeoff).

Authorization sidecar: H6 reads `<plan_dir>/cdcc-step-reexec-authorization.txt` rather than the agent's in-context state because Claude Code's PreToolUse hook protocol does not expose direct working-memory inspection. The sidecar is operator-authored or persona-authored under explicit operator authority. Documented as honest_gap in implementation audit log.
