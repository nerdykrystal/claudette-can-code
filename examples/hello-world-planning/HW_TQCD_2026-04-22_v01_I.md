---
document_type: Testing & Quality Criteria Document
project: Hello World SvelteKit App (Sandbox Validation Harness)
version: v01_I
created: 2026-04-22
status: Approved
paired_prd: HW_PRD_2026-04-22_v01_I.md
paired_trd: HW_TRD_2026-04-22_v01_I.md
paired_avd: HW_AVD_2026-04-22_v01_I.md
---

# TQCD — Hello World SvelteKit App

## Acceptance Criteria (Must Pass For BUILD COMPLETE)

| ID | Criterion | Verification |
|---|---|---|
| **AC-1** | The greeting "Hello, World!" is visible in the rendered page | Manual or automated DOM query; also covered by AC-5 |
| **AC-2** | The app builds without errors | `npm run build` exits 0 |
| **AC-3** | The app type-checks cleanly under strict mode | `npx tsc --noEmit` exits 0 |
| **AC-4** | ESLint produces zero violations | `npx eslint src/ --max-warnings=0` exits 0 |
| **AC-5** | The Greeting component renders the expected text | Vitest unit test in `Greeting.test.ts` passes |
| **AC-6** | 100% line + branch coverage of `Greeting.svelte` | `c8` output shows 100% on the component file |
| **AC-7** | Zero axe-core CRITICAL or SERIOUS violations on the built page | `axe <preview-url>` returns zero findings at those severity levels |
| **AC-8** | Lighthouse Accessibility score = 100 | `lighthouse <preview-url>` report |
| **AC-9** | Lighthouse Performance score ≥ 95 | Same report |
| **AC-10** | Lighthouse Best Practices score ≥ 95 | Same report |
| **AC-11** | The git repo has branded-terminology-only commit messages | `git log --format=%B \| grep -c <IP-expansion-pattern>` returns 0 |
| **AC-12** | The git repo has no file paths with IP expansions | `git ls-files \| grep -c <IP-expansion-pattern>` returns 0 |

All 12 acceptance criteria must pass for the D2R pipeline to emit BUILD COMPLETE.

## Test Categorization (per Software Testing Taxonomy pin)

Per `_experiments` skill bundle pin, Testing Taxonomy categories selected for this app:

| Category | Selected? | Rationale |
|---|---|---|
| Unit testing | YES | `Greeting.test.ts` — minimum one test |
| Integration testing | OPTIONAL | One integration test in `tests/integration/` is acceptable but not required |
| End-to-end testing | NO | Out of scope for this trivial app |
| Accessibility testing | YES | Required for AC-7, AC-8 |
| Performance testing | YES | Required for AC-9 |
| Best-practices scan | YES | Required for AC-10 |
| Security testing | NO | No attack surface |
| Load testing | NO | Static site |
| Contract testing | NO | No APIs |
| Property-based testing | NO | Trivial behavior |
| Fuzz testing | NO | No input |
| Mutation testing | NO | Overkill for one test |
| Snapshot testing | OPTIONAL | Component output is deterministic; snapshot would work but unit test is sufficient |
| Visual regression | NO | No visual complexity |
| Smoke testing | IMPLICIT | Covered by `npm run build` succeeding |

D2R pipeline may add additional tests per the Testing Taxonomy's AI-driven stress selection at its discretion, but AC-1 through AC-12 are the minimum bar.

## Coverage Requirements

- **Line coverage:** 100% of `Greeting.svelte`
- **Branch coverage:** 100% of `Greeting.svelte` (trivial — one branch maximum)
- **File coverage:** 100% of `src/lib/components/` (the only file there is `Greeting.svelte`)
- Other files (`+page.svelte`, `+layout.svelte`, config files) exempt from coverage requirement per SvelteKit convention

## ASAE Gates

Per D2R Stage NN-M Meta-ASAE, the following ASAE gates fire during the build:

| Gate | Stage | Domain | Threshold | Expected Outcome |
|---|---|---|---|---|
| Plan audit | 01a end | document | 2 | Clean — plan is trivially simple |
| Plan expansion audit | 01b end | document | 2 | Clean |
| Scaffold audit | 02 end | code | 2 | Clean |
| Implementation audit | 03 end | code | 3 | Clean (strict — code is load-bearing for AC) |
| QA audit | QA end | code | 3 | Clean |
| Meta-ASAE | Between each stage | meta | 2 | Clean |

If any gate fails to converge within its maximum iteration count, the pipeline HALTs and validation fails.

## Quality Score Pass Thresholds

| Metric | Minimum | Target |
|---|---|---|
| Lighthouse Performance | 95 | 100 |
| Lighthouse Accessibility | 100 | 100 |
| Lighthouse Best Practices | 95 | 100 |
| Lighthouse SEO | 90 | 100 |
| Line coverage (Greeting.svelte) | 100% | 100% |
| Branch coverage (Greeting.svelte) | 100% | 100% |
| TS errors | 0 | 0 |
| ESLint violations | 0 | 0 |
| axe-core CRITICAL + SERIOUS | 0 | 0 |

Any metric below minimum = validation failure.

## Failure Diagnosis

If validation fails, consult:

- `expected-artifacts.md` to identify which artifacts are missing or malformed
- `validation-checklist.md` for pass/fail breakdown per check
- The sandbox transcript for protocol execution errors

Before re-running the factorial, the failure must be diagnosed and fixed at the protocol level (Sandbox Setup Protocol, D2R skill, or measurement tooling) — not by weakening this TQCD.
